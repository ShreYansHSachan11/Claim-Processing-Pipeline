services:
  - type: web
    name: claim-processing-pipeline
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: "uvicorn src.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
